# 全项目审查修复处理状态

日期：2026-09-07。状态：**已按用户要求停止，整个修复清单尚未完成**。

## 范围与停止边界

用户先授权执行全项目审查修复，随后要求“当前问题处理完成后，把处理情况更新下，然后停止”。本次已完成当前 AI 生成所有权、取消和租约失效问题的代码收尾及本地验证，不再继续后续问题。当前问题的双库运行验证仍未完成，不标记 `Verified`。

修复基线及停止时 HEAD 为 `1af03bc0b2cf5ee10b47129ffd59ebc797e73272`，分支 `main`。任务快照为 `full-project-review-fixes-20260907`。停止后用户授权将本阶段修复及审查记录提交到本地 Git；本次提交不表示整个修复清单完成或完整门禁通过。未推送、未发布，也未将迁移应用到业务数据库。保留了任务开始前已有的审查文档。

输入：[原始架构与代码审查](2026-09-07-full-project-architecture-code-review.md)。执行清单：[活动修复计划](../superpowers/plans/2026-09-07-full-project-review-fixes.md)。原始审查报告保留为历史证据，本记录说明修复后的实际进展，不能反向修改原始评估事实。

## 当前 AI 问题的处理结果

- 数据库以 `GenerationId`、到期时间及取消标志控制生成所有权，获取槽位与初始消息写入处于同一短事务。
- 本地注册、取消、消息完成和释放均核对生成代次，旧请求的取消或 finally 不会影响新请求。
- 独立数据库作用域立即续租并每 5 秒检查，单轮等待最多 10 秒；本地独立租约期限在数据库不返回时仍取消外部推理。调用模型前、完成消息前都再次确认租约。
- HTTP 启动失败会释放已提交的槽位；消息写入失败回滚启动事务。过期状态不会让客户端永久等待，新生成会收敛遗留 streaming 消息。
- 新增 205 双库迁移，保留新版本有效租约；升级时停止旧 API 实例。SQL Server 对引用新增列的更新使用动态执行，避免同批编译错误。
- 配套补齐模型租户读取、204 配额预留/幂等结算、有限响应读取和 OpenAI `usage:null` 兼容。

独立只读复核发现的“迟到取消误伤后继”和“续租等待超过租约期限”均已修正并补测试。复核未发现这两项范围内的剩余明确阻断；静态复核不替代数据库运行验证。

## 18 组发现的实际进度

“本地回归通过”只覆盖已执行测试；“待验证”不得解释为已经通过完整门禁。

| 编号 | 处理情况 | 当前状态 / 剩余工作 |
| --- | --- | --- |
| R01 | 通知服务与 Endpoint 开关装配一致，开关矩阵测试通过 | 已修复并有本地回归；后续模块目录调整后的完整架构门禁仍需重跑 |
| R02 | ImportExport/Reporting 任务 SQL 增加可信租户绑定与谓词；Worker 按活动租户领取 | 17 条真实 ScopeGuard 回归通过；双库领取、并发与恢复验证待补齐 |
| R03 | 新增租户资源文件合同、所有权元数据与 202 双库迁移；替换导入/报表 Host 文件调用，保留受资源所有者确认的旧引用读取 | 文件测试 5 项通过；pending/ready 孤儿、释放后崩溃窗口及旧引用保留仍待 R17 收敛 |
| R04 | 支付/退款业务编号使用完整 Guid，避免截取 UUID v7 时间前缀造成碰撞 | 已修复，有固定时钟回归 |
| R05 | 支付、退款、K3Cloud、OCR 外部调用与本地事务分离，可靠外部意图与重试所有权 | **尚未修复，不得将资金/外部副作用可靠性认定为已解决** |
| R06 | 微信回调绑定商户、租户、订单、渠道、币种；保护退款等终态；验签原文、失败 HTTP 500；凭据唯一冲突回滚后重试 | 已修复；支付与地区聚焦 39 项通过 |
| R07 | 打印布局使用 DOMPurify 白名单净化，保留合法样式 | 攻击回归 2 项通过；现有 uni-app/Vite 依赖高危仍存在 |
| R08 | 限制秘密引用；验证码失败计数提交、发送脱离事务、串行挑战消费；投递租约与超时预算；按持久化模板版本重放 | 通知 147 项通过；真实双库锁竞争/投递故障窗口验证未执行 |
| R09 | AI 租户模型/配额消费 SQL 与 Host 管理 SQL 分离，可信租户绑定 | 实际服务与 ScopeGuard 回归通过；包含在最终 AI 33 项中 |
| R10 | 原子占用请求与 Token，204 预留凭据、原月份幂等结算；取消/未知用量保留预算；Guard 改 Scoped | 本地回归通过；真实双库并发、跨月及异常计量验证仍待补齐，未关闭整组 |
| R11 | 生成代次、持久化租约、启动事务、跨实例取消、本地期限、过期显示与旧代清理隔离 | **本次停止前已收尾的当前问题**；本地验证通过，双库持久化竞争测试已编写但未执行 |
| R12 | MQTT 保存完整正文 SHA-256 并比较幂等语义；201 双库迁移；Host 空租户查询匹配修正 | MQTT 9 项通过；MySQL 可空租户唯一索引的并发幂等窗口、真实迁移验证仍待收敛 |
| R13 | 16 份已发布 MySQL 脚本按全文摘要精确兼容；203 前向迁移修复 68 个 UUID 列，不修改历史文件 | 4 项兼容单元测试通过；双库升级/非法值/中间态测试已编写但未运行，文档外键及混合中间态覆盖仍待补齐 |
| R14 | 修复 AOT 参数、物化、源生成 JSON 与提供程序边界中的已发现静态错误 | 最终 AOT 分析通过，0 警告/0 错误；Linux 原生发布及进程运行未执行 |
| R15 | 修复请求 JSON、文件上传/下载、客户端合同、类型与 Vue 测试夹具 | 阶段性 Vue 构建成功、213 文件/722 测试通过，另有传输测试 2 项通过；最终前端/目录门禁尚未统一重跑 |
| R16 | 报表收集预算、行列上限、流式 worksheet、ZIP 写入限制；AI 帧/协议/正文限制、共享缓冲与有限历史 | 聚焦回归通过；不是内存基准或容量认证，真实大数据/外部提供程序验证未执行 |
| R17 | 修正导入旧数组执行状态读取；文件 pending 元数据与释放重试基础已加入 | **主要恢复工作未完成**：导入/报表执行租约、崩溃恢复、幂等重做与孤儿文件对账仍待处理 |
| R18 | 补全官方模块目录与 Platform 文件依赖；修正部分静态扫描、目录查找、Regions 参数/支付异常边界与 Jobs 夹具 | **尚未完成**：Identity/Tenancy 与 Files 依赖边界、Organization 导入扩展边界、Reporting 提供程序越界、目录/命名/生成图及部分注释规范仍待收敛 |

## 停止前新鲜验证

| 命令 | 实际结果 |
| --- | --- |
| `dotnet run --project tests/Full.NET.UnitTests --no-restore -- --filter 'FullyQualifiedName~Full.NET.UnitTests.Ai'` | 33 通过，0 失败，0 跳过 |
| `dotnet build tests/Full.NET.IntegrationTests --no-restore --nologo` | 编译成功，0 警告，0 错误；未运行数据库测试 |
| `pnpm test:aot:analyzers` | 成功，0 警告，0 错误；脚本已恢复默认 JIT 还原图 |
| `pnpm test:integration:affected:plan -- --snapshot full-project-review-fixes-20260907 --phase inner` | 已生成影响集；预计约 36 分钟，提示超出内循环预算，未执行该重型集合 |
| `git diff --check` | 通过；仅有 Git 换行转换提示 |
| `git rev-parse HEAD` / `git branch --show-current` / `git status --short` | 基线未变化，仍在 main，修复保留于未提交工作区 |

原始日志保存在 `artifacts/reviews/full-project-fixes-20260907/`：`ai-final-local.log`、`integration-build-final-local.log`、`aot-final-local.log`、`affected-plan-final-local.log`、`diff-check-final-local.log` 及阶段性红绿日志。该目录按仓库设置忽略，不能替代后续 CI 证据。

最近一次**阶段性**全量 Unit 为 2,349 通过、5 失败、1 Linux 专用跳过；之后对应模块装配与 Jobs 失败已分别修复并跑聚焦测试，但未重新运行全量，不能声明全量全绿。最近一次阶段性架构测试为 186 通过、22 失败，之后部分修正未重跑完整集合。治理曾为 50/52；模块依赖图、Global SQL/序列化目录、OpenAPI 与命名检查尚未全部收敛。

影响集选择器对新增 201–205 迁移及部分模块仍采用安全降级选择，后续须补登记；未通过放宽门禁来隐藏未完成项。按仓库规则，本次未启动 Docker、SQL Server/MySQL 容器、完整浏览器或 Linux Native AOT 重型环境，也未擅自提交/推送触发 CI。

## 继续时的边界

仅在用户重新要求继续后恢复工作。优先处理 R05 外部副作用可靠性、R17 任务恢复与文件对账、R12 Host 并发幂等、R18 实际依赖边界；随后补齐 R02/R03/R08/R10/R11/R13 的双库与故障验证，最后统一收敛治理、架构、前端及发布门禁。原始问题涉及的租户、事务和恢复不变量已有规则覆盖，本次修正实现，不新增近义规则或 Skill。
