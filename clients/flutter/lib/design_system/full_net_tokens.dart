import 'package:flutter/material.dart';

/// Full.NET 语义设计令牌；由 `packages/design-tokens/src/tokens.css` 映射，禁止手改漂移。
abstract final class FullNetTokens {
  static const ink = Color(0xFF17212B);
  static const inkMuted = Color(0xFF596670);
  static const sidebar = Color(0xFF172027);
  static const canvas = Color(0xFFF3F4EF);
  static const panel = Color(0xFFFFFEFA);
  static const line = Color(0xFFDFE4DF);
  static const accent = Color(0xFF08736D);
  static const accentBright = Color(0xFF42B9A6);
  static const signal = Color(0xFF936109);
  static const danger = Color(0xFFB83E3E);
  static const success = Color(0xFF20764F);

  static const radiusSm = 6.0;
  static const radiusMd = 12.0;
  static const spacingSm = 8.0;
  static const spacingMd = 16.0;
  static const spacingLg = 24.0;
}
